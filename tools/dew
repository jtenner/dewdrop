#!/usr/bin/env bash
set -euo pipefail
export DEW_WORKING_DIRECTORY="$PWD"
root="$(cd "$(dirname "$0")/.." && pwd)"
export DEW_REPOSITORY_ROOT="$root"
cd "$root"
exec moon run --target native --release src/dew_bootstrap -- "$@"
