pub enum Option<t> {
  None
  Some(t)
}

pub trait Iter<t> {
  fn next(self) -> Option<t>
}

pub fn dew_option_u32_from_raw(index: U32) -> Option<U32> {
  if index == 4294967295u32 {
    Option::None
  } else {
    Option::Some(index)
  }
}
