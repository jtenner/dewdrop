import {
  "jtenner/dewdrop/core_linker",
  "moonbitlang/core/debug",
  "moonbitlang/x/fs",
  "moonbitlang/x/sys",
}

pkgtype(kind: "executable")

options(
  link: { "native": { "cc": "gcc", "cc-flags": "-fwrapv -fno-strict-aliasing -O2 -pipe -flto=8" } },
)
