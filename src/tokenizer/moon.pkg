import {
  "moonbitlang/core/buffer",
  "moonbitlang/core/string",
  "moonbitlang/core/json",
}

options(
  targets: {
    "tokenizer_native.mbt": [ "native", "llvm" ],
    "tokenizer_stub.mbt": [ "wasm", "wasm-gc", "js" ],
  },
  "native-stub": [ "tokenizer_native.c" ],
)
