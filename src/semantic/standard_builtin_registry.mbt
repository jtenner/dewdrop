// Generated by tools/generate_standard_builtin_registry.py. Do not edit.

///|
enum StandardBuiltinOperation {
  NoStandardBuiltinOperation
  WasmArrayNew
  WasmArrayLen
  WasmArrayGet
  WasmArraySet
  FixedArrayMake
  FixedArrayLength
  FixedArrayGet
  FixedArrayGetUnchecked
  FixedArraySet
  ArrayNew
  ArrayWithCapacity
  ArrayLength
  ArrayCapacity
  ArrayGet
  ArrayGetUnchecked
  ArraySet
  ArraySetUnchecked
  ArrayPush
  ArrayPop
  ArrayClear
  ArrayIter
  ArrayIterHasNext
  ArrayIterNext
  StackNew
  StackWithCapacity
  StackLength
  StackIsEmpty
  StackPush
  StackPop
  StackPeek
  StackClear
  StackIter
  StackIterHasNext
  StackIterNext
  QueueNew
  QueueWithCapacity
  QueueLength
  QueueIsEmpty
  QueueEnqueue
  QueueDequeue
  QueuePeek
  QueueClear
  QueueIter
  QueueIterHasNext
  QueueIterNext
  CircularBufferBounded
  CircularBufferGrowable
  CircularBufferLength
  CircularBufferCapacity
  CircularBufferIsEmpty
  CircularBufferIsFull
  CircularBufferPushBack
  CircularBufferPopFront
  CircularBufferPeekFront
  CircularBufferClear
  CircularBufferIter
  CircularBufferIterHasNext
  CircularBufferIterNext
  CircularBufferPushFront
  CircularBufferPopBack
  CircularBufferPeekBack
  DequeNew
  DequeWithCapacity
  DequeLength
  DequeCapacity
  DequeIsEmpty
  DequePushFront
  DequePushBack
  DequePopFront
  DequePopBack
  DequePeekFront
  DequePeekBack
  DequeClear
  DequeIter
  DequeIterHasNext
  DequeIterNext
  MapSingleton
  MapLength
  MapGet
  MapGetUnchecked
  MapInsert
  MapSet
  MapContainsKey
  MapEmpty
  MapRemove
  MapClear
  MapKeys
  MapValues
  MapEntries
  MapKeyIterHasNext
  MapKeyIterNext
  MapValueIterHasNext
  MapValueIterNext
  MapEntryIterHasNext
  MapEntryIterNext
  MapEntryKey
  MapEntryValue
  SetEmpty
  SetSingleton
  SetLength
  SetIsEmpty
  SetContains
  SetInsert
  SetRemove
  SetClear
  SetIter
  SetIterHasNext
  SetIterNext
} derive(Eq, Debug)

///|
pub fn standard_fixed_array_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(9), 0)
}

///|
pub fn standard_fixed_array_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    "make" => 2
    "length" => 3
    "get" => 4
    "get_unchecked" => 5
    "set" => 6
    "set_unchecked" => 7
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(10), local_id)
  }
}

///|
pub fn standard_fixed_array_index_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(10), 9)
}

///|
pub fn standard_fixed_array_index_set_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(10), 11)
}

///|
fn standard_fixed_array_operation(
  declaration : DeclId,
) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(10) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 => FixedArrayMake
    3 => FixedArrayLength
    4 => FixedArrayGet
    5 | 9 => FixedArrayGetUnchecked
    6 | 7 | 11 => FixedArraySet
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_array_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(13), 0)
}

///|
pub fn standard_array_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    "new" => 17
    "with_capacity" => 18
    "length" => 19
    "capacity" => 20
    "push" => 22
    "pop" => 23
    "get" => 24
    "get_unchecked" => 25
    "set" => 26
    "set_unchecked" => 27
    "clear" => 28
    "iter" => 29
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(13), local_id)
  }
}

///|
pub fn standard_array_iterator_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(13), 1)
}

///|
pub fn standard_array_index_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(13), 31)
}

///|
pub fn standard_array_index_set_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(13), 33)
}

///|
fn standard_array_operation(declaration : DeclId) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(13) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 | 17 => ArrayNew
    3 | 18 => ArrayWithCapacity
    4 | 19 | 21 => ArrayLength
    5 | 20 => ArrayCapacity
    6 | 24 => ArrayGet
    7 | 25 | 31 => ArrayGetUnchecked
    8 | 26 => ArraySet
    9 | 27 | 33 => ArraySetUnchecked
    10 | 22 => ArrayPush
    11 | 23 => ArrayPop
    12 | 28 => ArrayClear
    13 | 29 => ArrayIter
    14 | 35 => ArrayIterHasNext
    15 | 36 => ArrayIterNext
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_stack_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(14), 0)
}

///|
pub fn standard_stack_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(14), local_id)
  }
}

///|
fn standard_stack_operation(declaration : DeclId) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(14) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 => StackNew
    3 => StackWithCapacity
    4 => StackLength
    5 => StackIsEmpty
    6 => StackPush
    7 => StackPop
    8 => StackPeek
    9 => StackClear
    10 => StackIter
    11 => StackIterHasNext
    12 => StackIterNext
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_queue_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(15), 0)
}

///|
pub fn standard_queue_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(15), local_id)
  }
}

///|
fn standard_queue_operation(declaration : DeclId) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(15) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 => QueueNew
    3 => QueueWithCapacity
    4 => QueueLength
    5 => QueueIsEmpty
    6 => QueueEnqueue
    7 => QueueDequeue
    8 => QueuePeek
    9 => QueueClear
    10 => QueueIter
    11 => QueueIterHasNext
    12 => QueueIterNext
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_circular_buffer_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(36), 0)
}

///|
pub fn standard_circular_buffer_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(36), local_id)
  }
}

///|
fn standard_circular_buffer_operation(
  declaration : DeclId,
) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(36) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 => CircularBufferBounded
    3 => CircularBufferGrowable
    4 => CircularBufferLength
    5 => CircularBufferCapacity
    6 => CircularBufferIsEmpty
    7 => CircularBufferIsFull
    8 => CircularBufferPushBack
    9 => CircularBufferPopFront
    10 => CircularBufferPeekFront
    11 => CircularBufferClear
    12 => CircularBufferIter
    13 => CircularBufferIterHasNext
    14 => CircularBufferIterNext
    15 => CircularBufferPushFront
    16 => CircularBufferPopBack
    17 => CircularBufferPeekBack
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_deque_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(37), 0)
}

///|
pub fn standard_deque_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(37), local_id)
  }
}

///|
fn standard_deque_operation(declaration : DeclId) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(37) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 => DequeNew
    3 => DequeWithCapacity
    4 => DequeLength
    5 => DequeCapacity
    6 => DequeIsEmpty
    7 => DequePushFront
    8 => DequePushBack
    9 => DequePopFront
    10 => DequePopBack
    11 => DequePeekFront
    12 => DequePeekBack
    13 => DequeClear
    14 => DequeIter
    15 => DequeIterHasNext
    16 => DequeIterNext
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_map_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 0)
}

///|
pub fn standard_map_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    "singleton" => 9
    "length" => 10
    "get" => 11
    "get_unchecked" => 12
    "insert" => 13
    "set" => 14
    "contains_key" => 15
    "empty" => 16
    "remove" => 17
    "clear" => 18
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(11), local_id)
  }
}

///|
pub fn standard_map_entry_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 26)
}

///|
pub fn standard_map_key_iterator_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 27)
}

///|
pub fn standard_map_value_iterator_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 28)
}

///|
pub fn standard_map_entry_iterator_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 29)
}

///|
pub fn standard_map_index_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 20)
}

///|
pub fn standard_map_index_set_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(11), 22)
}

///|
fn standard_map_operation(declaration : DeclId) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(11) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    1 | 9 => MapSingleton
    2 | 10 => MapLength
    3 | 11 => MapGet
    4 | 12 | 20 => MapGetUnchecked
    5 | 13 => MapInsert
    6 | 14 | 22 => MapSet
    7 | 15 => MapContainsKey
    16 | 23 => MapEmpty
    17 | 24 => MapRemove
    18 | 25 => MapClear
    30 | 42 => MapKeys
    31 | 43 => MapValues
    32 | 44 => MapEntries
    33 | 49 => MapKeyIterHasNext
    34 | 50 => MapKeyIterNext
    35 | 52 => MapValueIterHasNext
    36 | 53 => MapValueIterNext
    37 | 55 => MapEntryIterHasNext
    38 | 56 => MapEntryIterNext
    39 | 46 => MapEntryKey
    40 | 47 => MapEntryValue
    _ => NoStandardBuiltinOperation
  }
}

///|
pub fn standard_set_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(12), 0)
}

///|
pub fn standard_set_method_declaration(name : String) -> DeclId {
  let local_id = match name {
    "empty" => 10
    "singleton" => 11
    "length" => 12
    "is_empty" => 13
    "contains" => 14
    "insert" => 15
    "remove" => 16
    "clear" => 17
    "iter" => 23
    _ => -1
  }
  if local_id < 0 {
    0UL
  } else {
    make_semantic_id(standard_library_module_id(12), local_id)
  }
}

///|
pub fn standard_set_iterator_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(12), 18)
}

///|
fn standard_set_operation(declaration : DeclId) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(12) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    1 | 10 => SetEmpty
    2 | 11 => SetSingleton
    3 | 12 => SetLength
    4 | 13 => SetIsEmpty
    5 | 14 => SetContains
    6 | 15 => SetInsert
    7 | 16 => SetRemove
    8 | 17 => SetClear
    19 | 23 => SetIter
    20 | 25 => SetIterHasNext
    21 | 26 => SetIterNext
    _ => NoStandardBuiltinOperation
  }
}

///|
fn standard_wasm_intrinsics_operation(
  declaration : DeclId,
) -> StandardBuiltinOperation {
  if semantic_id_module(declaration) != standard_library_module_id(9) {
    return NoStandardBuiltinOperation
  }
  match semantic_id_local(declaration) {
    2 => WasmArrayNew
    3 => WasmArrayLen
    4 => WasmArrayGet
    5 => WasmArraySet
    _ => NoStandardBuiltinOperation
  }
}

///|
fn standard_option_type_declaration() -> DeclId {
  make_semantic_id(standard_library_module_id(2), 0)
}

///|
pub fn standard_option_none_variant() -> VariantId {
  make_semantic_id(standard_library_module_id(2), 0)
}

///|
pub fn standard_option_some_variant() -> VariantId {
  make_semantic_id(standard_library_module_id(2), 1)
}

///|
fn standard_lane_module_path(index : Int) -> String {
  match index {
    0 => "dew.std.i8x4"
    1 => "dew.std.u8x4"
    2 => "dew.std.i16x2"
    3 => "dew.std.u16x2"
    4 => "dew.std.i8x8"
    5 => "dew.std.u8x8"
    6 => "dew.std.i16x4"
    7 => "dew.std.u16x4"
    8 => "dew.std.i32x2"
    9 => "dew.std.u32x2"
    10 => "dew.std.i8x16"
    11 => "dew.std.u8x16"
    12 => "dew.std.i16x8"
    13 => "dew.std.u16x8"
    14 => "dew.std.i32x4"
    15 => "dew.std.u32x4"
    16 => "dew.std.i64x2"
    17 => "dew.std.u64x2"
    18 => "dew.std.f32x4"
    19 => "dew.std.f64x2"
    _ => abort("unknown standard lane module slot")
  }
}
