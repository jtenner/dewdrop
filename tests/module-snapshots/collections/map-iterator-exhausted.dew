open dew.std.map

pub fn main() -> Unit {
  let iter = Map::singleton(1, 10).keys()
  match iter.next() {
    Option::Some(value) => assert(value == 1, "first key")
    Option::None => unreachable()
  }
  match iter.next() {
    Option::Some(_) => unreachable()
    Option::None => {}
  }
}
