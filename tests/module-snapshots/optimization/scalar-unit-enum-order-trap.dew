enum State {
  Left
  Right
}

fn trap_condition() -> Bool {
  unreachable()
}

pub fn main() -> I32 {
  let state = if trap_condition() {
    State::Left
  } else {
    State::Right
  }
  match state {
    State::Left => 1
    State::Right => 2
  }
}
