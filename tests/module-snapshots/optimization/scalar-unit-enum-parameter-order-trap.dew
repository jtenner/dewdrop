enum State {
  Left
  Right
}

fn classify(state: State) -> I32 {
  match state {
    State::Left => 1
    State::Right => 2
  }
}

fn trap_condition() -> Bool {
  unreachable()
}

pub fn main() -> I32 {
  classify(if trap_condition() {
    State::Left
  } else {
    State::Right
  })
}
