open dew.std.wasi
fn narrow(value: U64) -> I16 {
  value.into()
}

fn widen(value: I16) -> I32 {
  value.into()
}

pub fn main() -> U32 {
  if widen(narrow(65535u64)) == -1 {
    wasi_fd_write(1u32, b"numeric:integer-conversion")
  } else {
    wasi_fd_write(1u32, b"FAIL")
  }
}
