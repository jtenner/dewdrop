open dew.std.string
open dew.std.wasi
open dew.std.bytes

pub fn main() -> U32 {
  let left = "xxabcdefghijklmnopqrstu".view(2u32, 17u32).to_string()
  let right = "YYrstuvwxyzZZ".view(2u32, 9u32)
  let result = left.concat_view(right)
  wasi_fd_write(1u32, result.as_bytes())
}
